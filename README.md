# remnote-cli

CLI companion app for RemNote Bridge via WebSocket.

## Status

Bootstrap release. This version provides a hello-world command so the package name can be claimed and installed.

## Install

```bash
npm install -g remnote-cli
```

## Usage

```bash
remnote-cli
```

Expected output:

```text
Hello from remnote-cli. WebSocket integration is coming next.
```
